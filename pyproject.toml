[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "inhibitory-behaviour-pipeline"
version = "0.1.0"
description = "Behaviour analysis pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]